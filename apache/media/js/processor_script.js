/* Functions for the page that displays processor families*/

$(function() {
    // Have interleaving colors for the rows of processors for a better look
    $(".processor_table tr:nth-child(odd)").addClass("odd-row");
});
